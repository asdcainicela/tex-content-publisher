%------------------------------------------------------------------------
% TEX-CONTENT-PUBLISHER - Preamble Base
% Generic template for technical documents
%------------------------------------------------------------------------

\documentclass[20pt]{extarticle}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{setspace}

% Math packages
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}

% Graphics and colors
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{pagecolor}
\usepackage{geometry}
\usepackage{hyperref}

% Headers/Footers
\usepackage[plainfootsepline]{scrlayer-scrpage}

% Fonts
\usepackage{mathpazo}
\usepackage{microtype}

% Tables and lists
\usepackage{listings, multicol}
\usepackage{wrapfig}
\usepackage[x11names,table]{xcolor}
\usepackage{colortbl}
\usepackage[most]{tcolorbox}

% Sections
\usepackage{titlesec}

%------------------------------------------------------------------------
% LOAD CONFIGURATION (with fallback to defaults)
%------------------------------------------------------------------------
\InputIfFileExists{config-colors}{}{\input{defaults/colors}}
\InputIfFileExists{config-logos}{}{\input{defaults/logos}}
\InputIfFileExists{config-layout}{}{\input{defaults/layout}}

%------------------------------------------------------------------------
% PAGE CONFIGURATION
%------------------------------------------------------------------------
\geometry{
    paperwidth=32cm,
    paperheight=32cm,
    margin=3cm,
    top=4cm
}

%------------------------------------------------------------------------
% HYPERREF SETUP
%------------------------------------------------------------------------
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,      
    urlcolor=blue,
}

%------------------------------------------------------------------------
% SECTION FORMATTING
%------------------------------------------------------------------------
\titleformat{\section}
    {\centering\color{ColorBlueText}\fontsize{40}{48}\bfseries}
    {}
    {0pt}
    {}

\titleformat{\subsection}
    {\color{ColorBlueText}\fontsize{31}{37}\bfseries}
    {}
    {0pt}
    {}

%------------------------------------------------------------------------
% SPACING
%------------------------------------------------------------------------
\setlength{\parskip}{15pt}
\setlength{\parindent}{0pt}

%------------------------------------------------------------------------
% USEFUL COMMANDS
%------------------------------------------------------------------------
\newcommand{\imp}[1]{\textcolor{red}{#1}}
\newcommand{\highlight}[1]{\fcolorbox{white}{white}{\lstinline|#1|}}
\newcommand{\Highlight}[1]{\lstinline|#1|}

%------------------------------------------------------------------------
% TCOLORBOX (content boxes)
%------------------------------------------------------------------------
\newtcolorbox{Box1}[2][]{
    lower separated=false,
    colback=ColorBox,
    colframe=white,
    fonttitle=\bfseries,
    colbacktitle=ColorBoxTitle,
    coltitle=black,
    enhanced,
    attach boxed title to top left={xshift=0.5cm,yshift=-2mm},
    title=#2,#1
}

% Debug command
\newcommand{\debugval}[1]{\fcolorbox{ColorDebugFrame}{ColorDebugFill}{\small{#1}}\ }
